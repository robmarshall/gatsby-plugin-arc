{
  "name": "gatsby-plugin-arc",
  "description": "Gatsby plugin to add Arc onto a site",
  "version": "0.0.1",
  "author": "Robert Marshall",
  "bugs": {
    "url": "https://github.com/gatsbyjs/gatsby/issues"
  },
  "dependencies": {
    "@babel/runtime": "^7.6.2",
    "minimatch": "^3.0.4",
    "gatsby": "^2.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.2",
    "@babel/core": "^7.6.2",
    "babel-preset-gatsby": "^0.2.17",
    "cross-env": "^5.2.1",
    "jest": "^24.9.0"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "arc",
    "cdn",
    "monitization"
  ],
  "license": "MIT",
  "peerDependencies": {
    "gatsby": "^2.0.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__",
    "test": "jest"
  }
}
